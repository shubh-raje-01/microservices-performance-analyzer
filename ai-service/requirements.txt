# Web framework 
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Validation & settings 
pydantic==2.7.1
pydantic-settings==2.3.0

# HTTP client 
# Used by LLMClient (Ollama calls) and health check router
httpx==0.27.0

# Machine learning 
# AnomalyDetector — IsolationForest, StandardScaler
scikit-learn==1.5.0
numpy==1.26.4

# Forecasting 
# LatencyForecaster — heuristic now, ARIMA ready when historical
# data is available
statsmodels==0.14.2

# scipy is a statsmodels transitive dependency but pinned explicitly
# so the version is predictable across environments
scipy==1.13.0

# LLM integration 
# LLMClient — Claude API backend
anthropic==0.28.0

# Retry logic 
# LLMClient @retry decorators on both Ollama and Claude paths
tenacity==8.3.0

# Observability 
# Exposes /metrics endpoint consumed by Prometheus
prometheus-fastapi-instrumentator==7.0.0

# Structured JSON logging — configure_logging() in app/core/logging.py
structlog==24.2.0

# Environment 
python-dotenv==1.0.1

# Testing 
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# httpx is already listed above — TestClient for FastAPI uses it
# respx mocks outbound httpx calls (Ollama health check, LLM calls)
respx==0.21.1